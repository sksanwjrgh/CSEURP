# 1) Matrix operations and Gauss-Jordan elimination
# Importing necessary libraries
import random
import numpy as np
import math

# Generating a sorted list of 100 unique random integers between 1 and 100000
list = np.sort(random.sample(range(1,100000),100))

# Pirnt list
print(list)